{
  "name": "booster",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "solution: build": "dotnet build",
    "solution: clean": "dotnet clean",
    "solution: test": "dotnet test",
    "solution: publish local packages": "tools/scripts/push-local-packages.sh",
    "docker: build": "docker compose -f platforms/local/docker-compose.yml build",
    "docker: up": "docker compose -f platforms/local/docker-compose.yml up -d",
    "docker: down": "docker compose -f platforms/local/docker-compose.yml down",
    "misc: add all projects to sln": "tools/scripts/add-all-projects-sln.sh"
  }
}
